#!/bin/bash

cd <%= node[:selenium][:install_path] %>
exec 2>&1
exec java -jar selenium-server-standalone.jar -role node -hub http://<%= node[:selenium][:hub][:domain] %>:<%= node[:selenium][:hub][:port] %>/grid/register #-port 5555 -host node[:ipaddress]
